<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Walmart Healthier Alternatives</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
<header class="app-header">
  <h1>Healthier Walmart Alternatives</h1>
  <button id="toggle-api" class="ghost">API Key</button>
</header>

<section id="api-panel" class="panel hidden">
  <label>
    OpenAI API Key
    <input type="password" id="apiKey" placeholder="sk-..." />
  </label>
  <button id="saveKey">Save</button>
  <small>Your key is only saved to your browser (localStorage).</small>
</section>

<main class="container">
  <aside class="sidebar">
    <h2>Your Goals</h2>
    <form id="goals-form">
      <div class="field">
        <label>Max Calories / serving</label>
        <input type="number" name="caloriesMax" value="300" min="0" />
      </div>
      <div class="field">
        <label>Min Protein (g)</label>
        <input type="number" name="proteinMin" value="20" min="0" />
      </div>
      <div class="field">
        <label>Max Sugar (g)</label>
        <input type="number" name="sugarMax" value="10" min="0" />
      </div>
      <div class="field">
        <label>Max Sat. Fat (g)</label>
        <input type="number" name="satFatMax" value="5" min="0" />
      </div>
      <div class="field">
        <label>Max Sodium (mg)</label>
        <input type="number" name="sodiumMax" value="600" min="0" />
      </div>

      <div class="field">
        <label>Delivery Method</label>
        <select name="fulfillment">
          <option value="any" selected>Any</option>
          <option value="pickup">Pickup</option>
          <option value="delivery">Delivery</option>
          <option value="shipping">Shipping</option>
        </select>
      </div>

      <button type="submit" class="primary">Save Goals</button>
      <button type="button" id="resetGoals" class="ghost">Reset</button>
    </form>
  </aside>

  <section class="content">
    <form id="analyze-form" class="card">
      <label for="url">Paste a Walmart product URL</label>
      <input id="url" type="url" required placeholder="https://www.walmart.com/ip/..." />
      <div class="actions">
        <button type="submit" class="primary">Find healthier alternatives</button>
        <button type="button" id="demoBtn" class="ghost">Use demo</button>
      </div>
    </form>

    <section id="results"></section>
  </section>
</main>

<template id="product-card">
  <article class="product card">
    <div class="score"></div>
    <h3 class="title"></h3>
    <p class="meta"></p>
    <ul class="nutrition"></ul>
    <a class="link" target="_blank" rel="noopener">Open on Walmart →</a>
  </article>
</template>

<script type="module" src="app.js"></script>
</body>
</html>
